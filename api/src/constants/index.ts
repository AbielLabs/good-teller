export enum HttpRoute {
  WITHOUT_PARAM = '/',
  WITH_ID_PARAM = '/:id',
}

export enum TransactionType {
  LIABILITY = 'liability',
  ASSETS = 'asset',
  DEBT = 'debt',
  EXPENSE = 'expense',
  REVENUE = 'revenue',
}
